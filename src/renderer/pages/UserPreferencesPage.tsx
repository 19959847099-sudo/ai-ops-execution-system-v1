export function UserPreferencesPage() {
  return (
    <section className="page-card">
      <p className="eyebrow">UserPreferencesPage</p>
      <h2>用户偏好页壳</h2>
      <p>当前只保留独立偏好结构的页面壳，不做完整读写流程，不混入系统设置。</p>
    </section>
  );
}
